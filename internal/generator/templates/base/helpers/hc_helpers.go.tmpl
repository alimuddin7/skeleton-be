package helpers

import (
	hModels "{{.ProjectName}}/helpers/models"
	"net/http"
)

func GenerateResponseHealthCheck(data ...hModels.DataHealthCheck) hModels.Response {
	return hModels.Response{
		Meta: hModels.MetaData{
			Code:    "2000000",
			Title:   "Available",
			Message: "Service is available",
		},
		Data: data,
	}
}
