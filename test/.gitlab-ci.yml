include:
  - local: .gitlab/ci/*.gitlab-ci.yml

stages:
  - sonar-scanner
  - build
  - deploy

variables:
  TRANSFER_FILES_DIR: "/home/ubuntu/$CI_PROJECT_NAME/transfer-files"
  DOCKER_DIR: ""
  PORT_SERVER: ""
  PORT_SERVER_DEV: "" # Destination server ==> 12.5 (2125) / 12.6 (2126)
  PORT_SERVER_STG: ""
